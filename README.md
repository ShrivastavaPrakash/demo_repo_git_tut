# Demo

Some description !

## SubHeader

Watch tutorial from FreeCodeCamp.


# git status command will show all of the file which is updated ,created or deleted but has not been saved in the comit yet 

# git add command will track all the files  with using period(.) which basiclly track all the files that are listed (generally developer use (.) ) but also optionlay tgell the gir to tarck a sepeic file by mention the name of each individual files (git add index.html or git add Readme.md)


# git commit -m(here -m is for message as we need to have some message in order to commit our file where the message could be of one char likr "a" may be meaningless or best what changes we same in short like idealy the message dels with what and why behind the commit we are making eg; -m "added delatils in reame file" or -m "added index.html"  so this is title for the message )

# and the second -m((for the discripion box as there were two box while commiting the file on git) -m"some description")
# git commit -m"redme conting command details" -m"command descriptions"  // this will give some details about how may inseration delation and changed files 
# here the  files are cahnged loaclly not on git

# git push - to make the cahnges we made live on git 
# git push origin master/main // where origin is an option set for us here  and basiclly a word that stands for the location of our git repositiory 
# while master/main is the branch we want to push to 



## Local Developement


1. open index.html in browser .